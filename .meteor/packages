# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base@1.0.4                   # Packages every Meteor app needs to have
mobile-experience@1.0.4             # Packages for a great mobile UX
mongo@1.1.10                         # The database Meteor supports right now
blaze-html-templates@1.0.4          # Compile .html files into Meteor Blaze views
session@1.1.6                       # Client-side reactive dictionary for your app
jquery@1.11.9                        # Helpful client-side library
tracker@1.1.0                       # Meteor's client-side reactive programming library

standard-minifier-css@1.1.8         # This package provides a minifier plugin used for Meteor apps by default.
standard-minifier-js@1.1.8          # This package provides a minifier plugin used for Meteor apps by default.
es5-shim@4.6.13                      # ECMAScript 5 compatibility for older browsers.
ecmascript@0.5.7                    # Enable ECMAScript2015+ syntax in app code

# autopublish                 # Publish all data to the clients (for prototyping)
# insecure                    # Allow all DB writes from clients (for prototyping)

random@1.0.10                        # The random package provides several functions for generating random numbers.
check@1.2.3                         # enables method arguments checks
audit-argument-checks@1.0.7         # This package causes Meteor to require that all arguments passed to methods and publish functions are checked.
reactive-var@1.0.10                  # This package provide ReactiveVar, a general-purpose reactive datatype for use with tracker.
reactive-dict@1.1.8                 # This package provide ReactiveDict, a general-purpose reactive datatype for use with tracker.
iron:router                   # A router that works on the server and the browser, designed specifically for Meteor
accounts-password@1.2.12             # email - password login provider
# accounts-ui                 # A turn-key user interface for Meteor Accounts.
underscore@1.0.9                    # underscore.js library
less@2.7.4                          # LESS extends CSS with dynamic behavior such as variables, mixins, operations and functions.
# markdown                    # basic markdown support
email@1.1.16                         # The email package allows sending email from a Meteor app.

anti:i18n                     # A simple (The simplest possible? Perhaps) i18n package for Meteor.
momentjs:moment               # A lightweight JavaScript date library for parsing, validating, manipulating, and formatting dates.
huttonr:bootstrap3            # Bootstrap 3 for Meteor - Modular, configurable, customizable.
useraccounts:bootstrap        # Meteor sign up and sign in templates (and much more!) styled for twitter Bootstrap
zimme:iron-router-auth        # Auth plugin and hooks for Iron.Router
alanning:roles                # Authorization package for Meteor - compatible with built-in accounts package.
houston:admin                 # Houston is a zero-config Meteor Admin, modeled after Django Admin.
sacha:spin                    # A Meteor package wrapper for Spin.js via Npm.depends(). - A spinning activity indicator http://spin.js.org.
chuangbo:marked               # A wrapper of marked. - A markdown parser and compiler. Built for speed.
perak:codemirror              # CodeMirror packaged for Meteor. CodeMirror is a versatile text editor implemented in JavaScript for the browser.
meteorhacks:kadira            # Kadira - Performance Monitoring for Meteor
# meteorhacks:meteor-down     # Meteor side package for MeteorDown load testing framework.
# mrt:publish-with-relations  # Publish with relations builds on Tom's gist (https://gist.github.com/tmeasday/4042603) to provide a convenient way to publish associated records.
sergeyt:typeahead             # Twitter's typeahead.js (http://twitter.github.io/typeahead.js/examples/) autocomplete package, wrapped for Meteor 1.0+.
percolate:synced-cron         # Allows you to define and run scheduled jobs across multiple servers
meteorhacks:ssr               # Server Side Rendering for Meteor with Blaze
pascoual:pdfkitx              # Create PDF with PDFKit Node.js package in your Meteor application, server-side and browser-side.

thrift                        # custom wrapper for Apache Thrift npm-package + Progressor implementation
npm-bcrypt@0.8.7
